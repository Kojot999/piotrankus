import { AppTheme } from "../types";

export const theme: AppTheme = {
  colors: {
    background: "#fff",
    foreground: "#fff",
    error: "#fff",
    font: "#fff",
    input: "#fff",
    primary: "#fff",
  },
};
